const logger = require('../../config/logger.config.js');

module.exports = {
    // get all users

    // get specific user

    // create a new user

    // update a user's details

    // delete (update the is_active flag) an existing user
}